#include <stdio.h>

/**
 *main - Entry Point
 * main: Prints a test
 * Return: Always 0 (Success).
 */

int main(void)
{
puts("\"Prpogramming is like building a multilingual puzzle\n");
return (0);
}
